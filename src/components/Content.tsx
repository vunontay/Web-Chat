import React from 'react';
import Conversations from '../Pages/Conversation';

const Content = () => {
    return (
        <div>
            <Conversations />
        </div>
    );
};

export default Content;
